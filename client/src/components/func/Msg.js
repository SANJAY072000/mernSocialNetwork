import React,{Component} from 'react';
import Navbar from '../Navbar';
import axios from 'axios';

export default class Msg extends Component{
    render(){
        return(
            <div>
            Welcome to Msg component.
            </div>
        );
    }
}